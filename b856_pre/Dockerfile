FROM python:3.11-slim

# Set working directory
WORKDIR /app

# Copy all project files
COPY . .

# The container will provide a development environment
# No build steps since this is a pure Python project with no dependencies

# Default to bash shell for interactive development
CMD ["/bin/bash"]